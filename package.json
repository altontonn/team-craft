{
  "scripts": {
    "build:css": "npx tailwindcss -i ./src/tailwind.css -o ./public/output.css --watch",
    "build": "npm run build:css && vite build"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.10"
  }
}
